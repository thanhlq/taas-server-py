# Project

eWorkSuite (ews) api by using fastapi framework.

This project is to publish all api for ews business, the generic api-route defined in libs/ews will be wired up depending on real api framework - fastapi.

All the configurations are put in .env file, normally located in taas-server-py/.env