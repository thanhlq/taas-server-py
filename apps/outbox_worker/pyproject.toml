[project]
name = "outbox_worker"
version = "0.1.0"
description = "Outbox relay worker: polls the transactional outbox and publishes pending events to the broker."
readme = "README.md"
authors = [
    { name = "Thanh LE", email = "thanh@floin.com" }
]
requires-python = ">=3.14"
dependencies = [
    # Core modules (workspace)
    'foundation',
    'db',
    'store_redis',
    'messaging_faststream',
    'resiliant',
    # Runtime
    'aiohttp>=3.9.0',
    'redis==7.4.0',
]

[project.scripts]
outbox_worker = "outbox_worker.main:main"

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest",
    "pytest-asyncio",
    "python-dotenv",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
pythonpath = ["."]
testpaths = ["tests"]
