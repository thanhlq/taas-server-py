services:
  ews_api:
    build:
      context: ./
      dockerfile: ./Dockerfile
    ports:
      - "8191:8191"
    volumes:
       - .:/app
       - /app/.venv
    command: sh -c "uv run ews_api python -m ews_api"
    env_file:
      - .docker.env

networks:
  default:
    name: external-network
    external: true
