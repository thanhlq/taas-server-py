# Resiliant

Implementations for service resilience in microservices - The purpose of this module is to implementation mechanisms similar as https://docs.temporal.io with concepts "Build applications that never fail"

Key points:

- These service interfaces are defined in platform_cores.reslient
- The database models are defined in libs/db/src/db/models/resiliant
- This package is normally implementation all interfaces defined in libs/foundation/src/foundation/resiliant

## Features

## dlq (Dead Letter Queue)

## Idempotency

## Outbox

All outbox implementation is put in outbox
